import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'secret love sex shop',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
